import Component from '@ember/component';
import layout from '../templates/components/freestyle-filterable-title';

export default Component.extend({
  layout,
  classNames: ['FreestyleFilterableTitle']
});
